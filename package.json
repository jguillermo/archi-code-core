{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/generate-packages-tree.js && node scripts/build-all.js",
    "test": "npm run test -ws --if-present",
    "test:coverage": "npm run test:coverage -ws --if-present",
    "test:scripts": "node --test scripts/__tests__/**/*.test.js",
    "check": "npm run check:format && npm run check:lint && npm run check:editorconfig",
    "check:lint": "npm run check:lint -ws --if-present",
    "check:format": "npm run check:prettier -ws --if-present",
    "check:editorconfig": "editorconfig-checker --exclude node_modules --exclude dist --exclude '.git'",
    "fix": "npm run fix:lint && npm run fix:prettier",
    "fix:lint": "npm run fix:lint -ws --if-present",
    "fix:prettier": "npm run fix:prettier -ws --if-present",
    "prepare": "husky",
    "clean": "rm -rf packages/*/node_modules node_modules",
    "generate-tree": "node scripts/generate-packages-tree.js",
    "release:dry-run": "node scripts/release-all.js --dry-run",
    "benchmark": "npm run benchmark -w packages/validation",
    "pr:ready": "npm run fix && npm run check && npm run test:coverage && git diff --exit-code"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/github": "^11.0.1",
    "@semantic-release/npm": "^12.0.1",
    "@semantic-release/release-notes-generator": "^14.0.3",
    "@types/jest": "29.5.14",
    "@types/node": "22.19.13",
    "@typescript-eslint/eslint-plugin": "8.56.1",
    "@typescript-eslint/parser": "8.56.1",
    "bcrypt": "^6.0.0",
    "editorconfig-checker": "^6.1.1",
    "eslint": "9.39.3",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-prettier": "5.5.5",
    "expect-type": "1.3.0",
    "fast-uri": "^3.1.0",
    "husky": "9.1.7",
    "jest": "29.7.0",
    "jsonwebtoken": "^9.0.3",
    "libsodium-wrappers-sumo": "^0.8.2",
    "semantic-release": "^24.2.3",
    "semantic-release-monorepo": "^8.0.2",
    "ts-jest": "29.4.6",
    "ts-node": "10.9.2",
    "typescript": "5.9.3",
    "typescript-eslint": "8.56.1"
  }
}
